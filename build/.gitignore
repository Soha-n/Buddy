# Build outputs and downloaded tooling — all regenerated by the scripts in
# scripts/, none of it belongs in the repository.
dist/
work/
payload/
release/
staging/
.tools/

# Scratch directories used when testing an assembled build locally.
apptest/
installtest/
testdata*/

*.zip
